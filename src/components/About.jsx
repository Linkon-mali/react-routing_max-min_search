import React from 'react'
import withLayout from '../hocs/withLayout';

const About = () => {
    return (
        <>
            <h2>Who are we?</h2>
        </>
      );
}

export default withLayout(About);